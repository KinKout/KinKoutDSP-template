### source folder contains all the source code of the plugin and template

- assets-helper : a useful folder for code that loads assets *(wraps audio, data, fonts, images etc.)*
- template : juce PluginEditor and PluginProcessor .txt used to generate .h and .cpp